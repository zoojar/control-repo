---
version: 5

lookup_options:
  "^profile::dashboard::.*(password|token)$":
    convert_to: Sensitive

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"
